#!/bin/sh /etc/rc.common
# Note: The text above has to be VERBATIM like that, or it won't be
# auto-starting!
SCRIPT=/lib/netkit/netkit-phase2
START=99

start() {
    . $SCRIPT start
}

stop() {
    . $SCRIPT stop
}

